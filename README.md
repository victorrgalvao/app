# Relógio React Native

App React Native simples que exibe a hora atual e atualiza a cada segundo.

## Como rodar

```sh
npm install
npm run start
```

Em outro terminal, execute a plataforma desejada:

```sh
npm run android
# ou
npm run ios
```
